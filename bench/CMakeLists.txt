find_package(benchmark QUIET)
if(NOT benchmark_FOUND)
  message(STATUS "Google Benchmark not found; skipping bench targets")
  return()
endif()

add_executable(mog-bench parser_bench.cpp)
target_include_directories(mog-bench PRIVATE ${CMAKE_SOURCE_DIR}/tests)
target_link_libraries(mog-bench PRIVATE mog_flags benchmark::benchmark pthread)

add_executable(mog-bench-book book_bench.cpp)
target_include_directories(mog-bench-book PRIVATE ${CMAKE_SOURCE_DIR}/tests)
target_link_libraries(mog-bench-book PRIVATE mog_flags benchmark::benchmark pthread)

add_executable(mog-bench-book-percentile book_percentile.cpp)
target_include_directories(mog-bench-book-percentile PRIVATE ${CMAKE_SOURCE_DIR}/tests)
target_link_libraries(mog-bench-book-percentile PRIVATE mog_flags)

find_path(MOG_VALGRIND_INCLUDE valgrind/callgrind.h)
if(MOG_VALGRIND_INCLUDE)
  foreach(ir_target book parser)
    set(ir_src book_percentile.cpp)
    if(ir_target STREQUAL "parser")
      set(ir_src parser_ir.cpp)
    endif()
    add_executable(mog-bench-${ir_target}-ir ${ir_src})
    target_compile_definitions(mog-bench-${ir_target}-ir PRIVATE MOG_IR_METER=1)
    target_include_directories(mog-bench-${ir_target}-ir PRIVATE ${CMAKE_SOURCE_DIR}/tests)
    target_link_libraries(mog-bench-${ir_target}-ir PRIVATE mog_flags)
  endforeach()
endif()

add_executable(mog-bench-sched scheduler_bench.cpp)
target_link_libraries(mog-bench-sched PRIVATE benchmark::benchmark_main mog_flags)
